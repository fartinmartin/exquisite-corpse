/**
 * One-time migration: Firestore (old Vue format) → canvas-paint DrawingData format.
 *
 * Old format: Firestore paths stored as Vue canvas draw commands
 * New format: canvas-paint { width, height, version, paths: Path[] }
 *
 * TODO: implement when Firestore credentials and old path format are confirmed.
 */

console.log("migrate-drawings: not yet implemented");
